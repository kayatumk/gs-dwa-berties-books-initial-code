# Berties Books

Sample code for Dynamic Web Applications module.

Uses node.js with Express and EJS.

To install and run:

npm install
node index.js

# dotenv

To install : npm install dotenv

Then use the command require('dotenv').config() at the top of index.js